name: CI - Exercicio 07 (Deteccao de Spam)

on:
  push:
    paths:
      - 'exercicio_07/**'
      - '.github/workflows/ex07_ci.yml'
  pull_request:
    paths:
      - 'exercicio_07/**'

jobs:
  ci:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: exercicio_07

    env:
      PYTHONPATH: ${{ github.workspace }}/exercicio_07

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install flake8
          pip install -r requirements.txt

      - name: Lint with flake8
        run: flake8 app/ src/ tests/

      - name: Train model
        run: python src/train.py

      - name: Run tests
        run: pytest tests/ -v

      - name: Build Docker image
        run: docker build -t exercicio_07 .

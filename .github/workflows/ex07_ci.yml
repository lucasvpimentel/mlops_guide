name: CI - Exercicio 07
on:
  push:
    paths:
      - 'exercicio_07/**'
  pull_request:
    paths:
      - 'exercicio_07/**'

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest
          if [ -f exercicio_07/requirements.txt ]; then pip install -r exercicio_07/requirements.txt; fi

      - name: Lint with flake8
        run: flake8 exercicio_07/

      - name: Test with pytest
        run: pytest exercicio_07/

      - name: Build Docker image
        run: docker build -t ex_07 ./exercicio_07
